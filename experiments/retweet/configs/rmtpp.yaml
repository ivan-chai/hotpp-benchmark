defaults:
  - default
  - module/head_partial: head_256
  - module/autoreg_adapter_partial: default
  - _self_

name: rmtpp

module:
  _target_: esp_horizon.modules.NextItemModule
  loss:
    _target_: esp_horizon.losses.NextItemLoss
    losses:
      timestamps:
        _target_: esp_horizon.losses.TimeRMTPPLoss
        max_delta: ${max_time_delta}
        init_influence: -0.1
        max_intensity: 10
        grad_scale: 0.05
        expectation_steps: 100
      labels:
        _target_: esp_horizon.losses.CrossEntropyLoss
        num_classes: ${num_classes}
  autoreg_adapter_partial:
    _target_: esp_horizon.modules.autoreg.NextItemRNNAdapter
  autoreg_max_steps: ${max_predictions}
