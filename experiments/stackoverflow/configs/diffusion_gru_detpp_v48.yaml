# Backwards-compat alias.
# Renamed to diffusion_gru_detpp_all_comps_in_decoder_far_target.yaml.
# Kept as a thin Hydra-defaults stub so legacy configs (V49, V50, V51)
# and scripts that reference `diffusion_gru_detpp_v48` keep working.
defaults:
  - diffusion_gru_detpp_all_comps_in_decoder_far_target
  - _self_

name: diffusion_gru_detpp_v48
