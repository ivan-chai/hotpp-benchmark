defaults:
  - detection_base
  - _self_

detection_k: 32

module:
  loss:
    loss_subset: 1
    prefetch_factor: 1
    drop_partial_windows: false
    match_weights:
      _presence: 4
      timestamps: 0.4
      labels: 0.8
